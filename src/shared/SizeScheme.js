/**
 * Standard size types for image and video generation models
 */
const SIZE_TYPES = {
  // Fixed size options (array of specific dimensions)
  FIXED: 'fixed',
  
  // Range with min and max values
  RANGE: 'range'
}

export { SIZE_TYPES } 